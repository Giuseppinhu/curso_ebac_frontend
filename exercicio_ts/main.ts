const multiplier = (value: number, value2:number) => (value * value2);

const sayHello = (name: string) => ("Hello! Developer " + name + " 🙂");

console.log(`O resultado da multiplicação deu: ${multiplier(2,2)}`)
console.log('\n')
console.log(sayHello("Giuseppe"))